apiVersion: v1
kind: Service
metadata:
  name: vertex-openai-proxy
  labels:
    app: vertex-openai-proxy
    app.kubernetes.io/name: vertex-openai-proxy
spec:
  selector:
    app: vertex-openai-proxy
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
